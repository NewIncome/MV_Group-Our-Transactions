<% provide(:title, 'Signup') %>

<%= form_for @user, html: { class: "mt-5" } do |f| %>

  <%= render  'shared/form_errors' %>

  <%= f.text_field :name, class: "form-control", placeholder: "Name" %>

  <%= f.submit class: "btn btn-primary my-4 mx-auto d-block" %>
<% end %>